# Template — create actual secret with:
#   kubectl create secret generic pubflow-secrets -n pubflow \
#     --from-literal=DATABASE_URL="postgresql://..." \
#     --from-literal=MINIO_ACCESS_KEY="..." \
#     --from-literal=MINIO_SECRET_KEY="..." \
#     --from-literal=KEYCLOAK_CLIENT_SECRET="..." \
#     --from-literal=KEYCLOAK_ADMIN_PASSWORD="..." \
#     --from-literal=ONLYOFFICE_JWT_SECRET="..." \
#     --from-literal=STRIPE_SECRET_KEY="sk_live_..." \
#     --from-literal=STRIPE_WEBHOOK_SECRET="whsec_..." \
#     --from-literal=STRIPE_PRICE_PROFESSIONAL="price_..." \
#     --from-literal=STRIPE_PRICE_ENTERPRISE="price_..." \
#     --from-literal=CROSSREF_LOGIN_ID="..." \
#     --from-literal=CROSSREF_LOGIN_PASSWORD="..."
apiVersion: v1
kind: Secret
metadata:
  name: pubflow-secrets
  namespace: pubflow
type: Opaque
# stringData:  # fill via kubectl or external-secrets-operator
